// import React from 'react';
// import { render, fireEvent } from '@testing-library/react';

// import { Input } from './Input';

// test('it should have label', () => {});

// test('it should show error message', () => {});

export const x = (y: any) => {
  y + 1;
};
